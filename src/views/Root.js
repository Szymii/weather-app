import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.main``;

const Root = () => {
  return (
    <Wrapper>
      <button>Hello</button>
    </Wrapper>
  );
};

export default Root;
